pyspark==4.0.4

requests==2.34.2
python-dateutil==2.9.0.post0
pandas==2.2.2

shapely

pyarrow==25.0.1
python-dotenv==1.2.2
pytest==8.4.2

# S3 마이그레이션: BRONZE/SILVER/GOLD 경로가 로컬 대신 S3에 있다.
# cloudpathlib가 pathlib 인터페이스(glob/mkdir/exists/unlink 등)를 그대로
# 흉내내주고, pandas I/O는 str(path)로 넘겨 s3fs가 처리한다.
boto3
cloudpathlib[s3]
s3fs

# DynamoDB 로컬 모킹 테스트용(src/common/dynamodb.py, src/common/emr_serverless.py 테스트).
moto[dynamodb]

# RDS(Postgres) 서빙 테이블 읽기/쓰기(src/common/db.py)에 사용.
psycopg2-binary
sqlalchemy

# TLC Bronze 데이터 품질 검증(Great Expectations)에 사용. 1.20.0 미만은
# pyspark==4.0.4와의 호환성을 검증하지 않았으므로 이 하한을 지킨다.
great_expectations>=1.20.0

# src/serving/api.py의 내비게이션 API가 직접 사용한다. apache/airflow 베이스
# 이미지에 Airflow 자체 의존성으로 딸려 들어와 있어서 없어도 동작하지만,
# 베이스 이미지가 바뀌면 조용히 깨질 수 있어 명시한다. 버전은 안 박아서
# 이미 설치된 버전(Airflow가 요구하는 버전)과 충돌하지 않게 한다.
# 세그먼트 지표 서빙 API(src/serving/nav_api.py)가 씀.
fastapi
uvicorn
PyYAML

# FastAPI TestClient(starlette.testclient)가 내부적으로 사용 — 테스트 전용.
httpx
